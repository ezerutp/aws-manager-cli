# AWS Manager CLI - Python Requirements
# Version is managed in src/__init__.py

# HTTP requests (for getting public IP)
requests>=2.31.0

# Build tool (optional, only needed for creating binary)
pyinstaller>=6.0.0